Convert the 10 different structured descriptions of the connetions between the components on a microfluidic chip, provided as input in a JSON format, into 10 natural language prompts suitable for prompting a specially fine-tuned LLM capable of creating microfluidic chip designs.

# Important Guidelines

1. Ensure that all possible details provided to a component must be included in the generated prompt. How you incorporate these details is up to you—feel free to experiment with different approaches, just as a real user would, to ensure all relevant information is captured.

# Steps

1. Apply all important guidelines.
2. Use varied introductions that resemble how real users might ask a conversational AI for designing a microfluidic chip.
3. Use synonyms for microfluidic components that may be mentioned in the prompt and replace repeated words and formulations with varied expressions that still keep the meaning clear and sound more conversational.
4. Convert the prompt into different perspectives.

# Output Format

The output should be a JSON containing a field for each prompt in the same order as the input descriptions:

```json
{
  "prompts": [
      {"prompt": "[Generated prompt 1]"},
      { "prompt": "[Generated prompt 2]"},
      ...
      {"prompt": "[Generated prompt 10]"}
  ]
}
```

# Notes

- Use different approaches across the input descriptions to cover as many ways as possible that a real user might prompt the LLM, ensuring a diverse and realistic range of phrasing styles.
- Each description should result in its own natural language prompt. Descriptions must not be combined. Ensure that each structured description is converted into a distinct and individual prompt that reflects its specific details.
- Ensure clarity while maintaining the natural flow expected in conversational language.
- Droplet formation involves two key phases: the dispersed phase, which is the fluid broken into droplets, and the continuous phase, which surrounds and supports the droplets, maintaining their structure and flow within the system.
- No component or step in the input description should be left out in the generated prompt, ensuring comprehensive coverage of all details provided.
- Ensure you adhere to the important guidelines.